
    /// Write a C program to insert element in array at specified position.

    #include <stdio.h>
    int main () {
        int n, i, pos, element, ar[50];
        printf("Enter size of array under <50: ");
        scanf("%d", &n);

        for (i=0; i<n; i++) {
            scanf("%d", &ar[i]);
        }
        printf("Enter element to insert and its position: ");
        scanf("%d %d", element, pos);




    }
